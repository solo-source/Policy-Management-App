package com.pms.frontend.model;

public class Customer {
    private Long id;
    // Additional fields like name, email, etc. can be added if needed

    // Getters and setters
    public Long getId() {
        return id;
    }
    public void setId(Long id) {
        this.id = id;
    }
}
